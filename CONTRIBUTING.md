# Contributing

**This repository is generated.** The plugin is developed alongside the Gridline API it
describes, so that a change to the API and the prose describing it land in one commit, and
it is mirrored here whenever its version is bumped.

Editing files here will be overwritten on the next release. Please open issues and pull
requests at <https://github.com/get-gridline/gridline>.
