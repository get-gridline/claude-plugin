# Contributing

**This repository is generated.** The plugin is developed alongside the Gridline API it
describes, so that a change to the API and the prose describing it land in one commit, and
a release arrives here as a pull request.

Edits made here are overwritten by the next release. Please open issues and pull requests
at <https://github.com/get-gridline/gridline>.
