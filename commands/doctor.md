---
description: Diagnose a Gridline setup — what is configured, half-configured, or wired to nothing
---

Run the `gridline-doctor` skill.

Check everything, read-only, and finish with a ranked list of fixes — most broken first. Do not
change anything; this command only reports.

$ARGUMENTS may name a single project. With no argument, check every project the credential can see.
