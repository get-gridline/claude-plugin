---
description: What Gridline spend went where, for a period — by tenant, agent, model or session
---

Run the `gridline-cost` skill for the period in $ARGUMENTS (for example `last week`, `this month`,
`yesterday`, `2026-07`). With no argument, use the last 7 days and say so.

Report the waste line alongside the total rather than netted into it, and surface
`unpriced_attempts` as a visible gap rather than dropping it.
